# Auto detect text files and normalize line endings
* text=auto

# Treat all HTML, CSS, and JS files as UTF-8 text
*.html text
*.css  text
*.js   text

# Mark Markdown files as text
*.md   text

# Exclude minified JS/CSS from GitHub linguist stats
*.min.js linguist-vendored
*.min.css linguist-vendored

# Ensure consistent handling for JSON
*.json text

# Prevent merge conflicts in package-lock.json (if added later)
package-lock.json merge=union
